/* Declaracipn de fuciones */

function suma(a, b) {
  return a + b;
}

console.log(`La suma es:  ${suma(4, 6)}`);

function naciones(pais) {
  console.log(`El país es: ${pais}`);
}
let bandera = naciones("Argentina");

/* Declaración de funciones */
naciones("Mexíco");
